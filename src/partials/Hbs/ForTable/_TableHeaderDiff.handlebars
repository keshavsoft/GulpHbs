{{#each columns}}
<th data-field="{{field}}" {{#if align}}data-align="{{align}}" {{/if}} {{#if filterControl}}data-filter-control="select"
    {{/if}}>
    {{#if title}}{{title}}{{/if}}
</th>
{{/each}}